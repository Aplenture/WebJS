export enum ButtonType {
    Default = 'default',
    Back = 'back',
    Settings = 'settings',
    Delete = 'delete',
    Cancel = 'cancel',
    Expose = 'expose',
    Copy = 'copy',
    Add = 'add'
}